# Map + list explore screen (2026-09-16)

## Status: WORKING (web + iOS), first pass

## Audit findings

**`public.events` already has usable coordinates**: `lat float8, lng float8` (`supabase/migrations/20260906000001_001_core_schema.sql:72-73`), alongside `cat_key`/`cat_label`/`area`/`status`/`visibility`/`seats_remaining`/`starts_at`. Real rows exist for every demo event (`supabase/migrations/20260910000020_020_seed_demo_events_for_test_accounts.sql`), plus any organizer-created event via the real Create Event flow.

**No map anywhere in the app before this** — grepped for `mapkit`/`mapbox`/`maplibre`/`MKMapView`/`leaflet`/`googlemaps` across `src/` and `apps/ios/`: zero hits. `apps/ios/BanbeApp/Services/LocationService.swift` exists but is a one-shot `CLLocationManager` wrapper only ever used to fill in a distance-from-you string on the feed/event-detail screens (mirrors web's `requestFreshCoords()`/`s.userCoords`/`s.located` in `GocContext.jsx:1901-1914`) — reused here, not replaced, for the compass button's underlying location fetch.

**Important architectural fact that changes this screen's data source**: the Home feed the rest of the app shows is driven entirely by a **static, in-memory** catalogue (`src/data/events.js`'s `EVENTS`, built from a hardcoded `ROWS` array) — not a live query. Every event there already carries a `lat`/`lng` (`areaCoords(area) + seededJitter(key)`, `events.js:117-140,192-197`), a `catKey` (matching `FILTER_DEFS` in `src/screens/Home.jsx:7-13` — reused as-is for this screen's category bar, no new taxonomy invented), and a `km` placeholder distance. The 020 migration above turns each of these same event keys into a **real** `public.events` row too (comment there: "so the client can join real assignment data back onto the frontend's rich cosmetic fields... which have no equivalent columns here and are intentionally not duplicated into the database"). This screen queries the **real table** (for live `seats_remaining`/`status`/pagination/bbox filtering — the whole point of the "real-time-ish" requirement) and joins each row's cosmetic fields (photo, description, price display) back from the static catalogue via `findEvent(row.id)`, falling back to a plain rendering using only the DB row's own columns for a real user-created event that has no static-catalogue counterpart at all.

**Category/filter model reused as-is**: `FILTER_DEFS` (`src/screens/Home.jsx:7-13`) — `all`/`supper`/`fashion`/`gallery`/`music`. No icon set exists anywhere for these (grepped `theme.js`/`Home.jsx`/`events.js` for `icon`/`emoji`/`glyph` — zero hits), so the ticket's "matching this app's existing category icon set if one exists" has nothing to match; picked plain, minimal glyphs (SF Symbols on iOS: `fork.knife`/`tshirt`/`paintpalette`/`music.note`; simple inline monochrome shapes on web) rather than inventing a new illustrated icon language the rest of the app doesn't have.

**Disabled-button opacity token, confirmed reused (not invented)**: `apps/ios/BanbeApp/Views/Components.swift:151` (`InkButton`) — `enabled ? app.palette.ink : app.palette.ink.opacity(0.16)`. Web's own disabled-submit convention (`src/screens/Login.jsx`'s `loginBtnStyle`) uses the identical value, `rgba(27,25,22,0.16)`. **0.16 is the one cross-platform "disabled" opacity value this app already has** — used for the dimmed compass button on both platforms.

**Poll cadence reused**: `src/state/GocContext.jsx:667`'s `setInterval(poll, 5000)` (the toast/notification poll, note 07) is the established general-purpose freshness interval — reused for this screen's list/pin refresh, rather than inventing a new cadence.

## Map stack — confirmed free, no billing risk

- **iOS**: native `MapKit` (SwiftUI `Map` + custom `Annotation` views) — no third-party dependency, no key, no usage cap of any kind; it's a system framework.
- **Web**: `maplibre-gl` (npm, MIT-licensed — the library itself is always free regardless of tile source) + **OpenFreeMap** as the tile/style source. Confirmed live and requiring no key/credit card by actually curling it from this session (not just recalled): `curl -sSI https://tiles.openfreemap.org/styles/positron` → `HTTP/2 200`, `access-control-allow-origin: *`, served via Cloudflare cache, no auth header of any kind accepted or required. OpenFreeMap's entire stated purpose is zero-cost, unlimited-usage MapLibre style+tile hosting (PMTiles-based, funded independently of per-request billing) — there is no metering to exceed. Picked the `positron` style specifically (light, minimal basemap) as the closest existing OpenFreeMap style to this app's warm-paper/ink aesthetic; `liberty` and `bright` are the other two published styles, also confirmed live (200).

## Bottom sheet — component chosen per platform

- **iOS**: native `.sheet(isPresented:) { ... }.presentationDetents([.height(peek), .medium, .large]).presentationDragIndicator(.visible)` (iOS 16+; this project's deployment target is iOS 17, `project.yml:4-5`) — gives drag-to-resize, a native grabber handle, and smooth follow-finger snapping entirely for free. No custom gesture code needed here, unlike the web side.
- **Web**: no existing bottom-sheet component or library in this codebase (confirmed via the same grep sweep as the map search) and no new dependency pulled in for it — built a small custom draggable sheet (`src/screens/MapExplore.jsx`'s `BottomSheet`) using pointer events + a CSS `transform: translateY()`, the same "live-follow-finger, no implicit animation mid-drag, snap on release" approach this session already used for iOS's custom edge-swipe gesture (note 09's swipe-back fix) — translated to the web/pointer-event equivalent rather than a new pattern.

## New facts found while implementing

- **`public.events` RLS already scopes to `status = 'live'` for anon/authenticated** (`001_core_schema.sql:122-125`) — the map's real-DB query just adds `.eq('status', 'live')` explicitly (redundant with RLS but makes the client-side intent explicit) rather than needing any new policy.
- **`FILTER_DEFS` exported from `Home.jsx`** (was module-private) so `MapExplore.jsx` reuses the exact same array instead of a duplicate taxonomy — per the ticket's "don't duplicate a separate taxonomy" instruction.
- **New context actions, not new context state**: `goMapExplore`/`backFromMapExplore` (`GocContext.jsx`, next to `goHome`) are the only two additions to the shared context — everything else the screen needs (sheet snap position, category/sort filters, map viewport, loaded events) is local `useState` inside `MapExplore.jsx` itself, since none of it needs to survive leaving the screen or be read by any other screen. `s.userCoords`/`s.located`/`allowLocation()` (already in context, from the existing location-sheet feature) are reused as-is rather than duplicated.
- **iOS mirrors the same split**: `AppState.mapEvents`/`mapEventsLoading`/`locationAuthStatus` are the only new `@Published` state (needed because `AppState` is the one place that owns the Supabase client and `LocationService`); sheet detent and filter state are plain `@State` inside `MapExploreView`.
- **`LocationService` extended, not replaced**: added `authorizationStatus` (read), `isPermanentlyDenied`, `onAuthorizationChange` callback, and a static `openSettings()` — the existing one-shot `request()`/`onUpdate` flow used by the feed's distance strings is untouched and still used by the compass button for the actual location fetch.
- **New model**: `MapEventRow` (`Models/Event.swift`) — a narrower projection of `events` carrying `cat_key` (which the existing `Event` model doesn't have any use for elsewhere, so wasn't added there).
- **Supabase-swift gotcha**: `.gte`/`.lte` are only available on `PostgrestFilterBuilder` (pre-`.order()`/`.limit()`); calling them after `.order()`/`.limit()` (which return `PostgrestTransformBuilder`) fails to compile with "value of type 'PostgrestTransformBuilder' has no member 'gte'". Fixed by building the filter chain first, applying `.order()`/`.limit()` last, right before `.execute()`.
- **SwiftUI persistent bottom sheet**: no first-class API exists for "a bottom sheet permanently up over full-screen content" pre-iOS 26, so `MapExploreView` uses the standard `.sheet(isPresented:)` API with `isPresented: .constant(true)` + `.interactiveDismissDisabled()` + `.presentationBackgroundInteraction(.enabled)` (so the map underneath still receives pan/zoom while the sheet is up) — a known trick, not a new pattern invented here.
- **Compass recenter is still user-tap-only even across a fresh permission grant**: the compass button can trigger `allowLocation()` (which starts the OS prompt), and once `userCoords` lands afterward the map recenters immediately without a second tap — but only because the tap itself set a one-shot `awaitingRecenterAfterGrant` flag first. A background poll/coords refresh alone never moves the map; the compass tap is still the only path that can.
- **Verification performed**: `npx vite build --mode production` (web) — clean, `maplibre-gl` code-splits into its own ~477KB chunk via the dynamic `import()`, not bundled into the main entry. `tests/density-hotspot.spec.js` (new, 4 cases covering empty/null input, single point, densest-cluster-over-outlier, and stable tie-breaking) — all pass. Fast Playwright suite (`--grep-invert "real backend"`) — 275/276 passed; the one webkit failure (`auth-notifications.spec.js`, a different test each run) reproduces identically on the pre-existing `main` branch under parallel workers and passes clean with `--workers=1`, confirming it's pre-existing webkit worker flakiness, not a regression from this change. iOS: both Debug and Release `xcodebuild -sdk iphonesimulator` builds succeed after `xcodegen generate` (new file `MapExploreView.swift` needed re-generating `BanbeApp.xcodeproj`).

## Follow-up fixes (2026-09-16): always-visible map strip, iOS thumbnail, web blank map

**UX restructure**: the map was already architecturally "always visible" (a full-screen `mapDivRef` div sits behind everything; the sheet only visually overlays part of it, so a strip is always uncovered above the sheet — `tall`=0.30 leaves 70% for the sheet/30% map, `peek`=0.86 leaves 86% map/14% sheet, neither ever fully covers the other). What was missing was an explicit, always-present control — previously the three snap points (`tall`/`mid`/`peek`) were reachable only by dragging the handle. Added two buttons directly below the drag handle in `src/screens/MapExplore.jsx` (`data-testid="map-sheet-list-large"` / `"map-sheet-list-small"`) that jump straight to `tall` ("List lớn" — today's default, list large/map small strip) and `peek` ("List nhỏ" — list small/map large, the state that used to be reachable only by dragging all the way down) respectively. The drag gesture itself is untouched and still works as an additional way to move between all three snap points, `mid` included. No iOS change needed here — `.presentationDetents` already exposes its snap points as both a drag gesture *and* is trivially bindable to buttons, but the ticket's two-explicit-buttons requirement was written against the web build's drag-only sheet specifically (iOS's native sheet already lets you tap the grabber area or drag; adding redundant buttons there wasn't asked for and would duplicate what `.presentationDetents` already gives for free).

**Bug 1 (iOS list thumbnail missing) — root cause**: `MapExploreView.swift`'s list row never had an image element at all — just `name`/`area` text. Every other event card in the app draws its photo via `CatalogPhoto(path:height:width:cornerRadius:)` (`Components.swift:6-37`, which wraps `RemoteImage`/`PhotoLoader`), but the map screen's own `MapEventRow` (a live-DB projection, `Models/Event.swift`) has no `img` field to draw from — the web build solves the same gap by joining the DB row back onto the static catalogue via `findEvent(row.id)` (`MapExplore.jsx`'s `fetchLiveEvents`). iOS needed the equivalent join, which didn't exist. **Fix**: `MapExploreView.swift`'s list row now does `EventCatalog.find(ev.id)?.img` and renders it through `CatalogPhoto(path:, height: 52, width: 52, cornerRadius: 10)` — the exact same component/loader path (`PhotoLoader` memory+disk cache, WebP-first, downsampled decode) every other card in the app already uses, not a new one.

**Bug 2 (web map blank) — root cause, confirmed by actually running the dev server under Playwright and reading the console, not guessed**: `maplibre-gl` 6.x ships pure named ESM exports with **no `default` export** (confirmed directly: `Object.keys(await import('maplibre-gl'))` lists `Map`, `Marker`, `NavigationControl`, etc. at the top level; `.default` is `undefined`). `MapExplore.jsx` did `const maplibregl = (await import('maplibre-gl')).default` — `maplibregl` was `undefined`, so `new maplibregl.Map(...)` threw `Cannot read properties of undefined (reading 'Map')` inside an async IIFE with no visible error surface, leaving the map container permanently blank (confirmed via `page.on('pageerror', ...)` in a throwaway Playwright script). The container-height theory from the original ticket (bounding box measured `1280×720` — never zero) turned out not to be the cause here. **Fix 1**: both dynamic-import call sites in `MapExplore.jsx` now use the module namespace directly (`const maplibregl = await import('maplibre-gl')`) instead of `.default`.

**A second, compounding cause surfaced once the above was fixed**: with a real `maplibregl.Map` constructed, the canvas and attribution control appeared but the basemap itself stayed visually blank — `page.on('requestfailed', ...)` showed `.../node_modules/.vite/deps/maplibre-gl-worker.mjs net::ERR_FAILED`. MapLibre loads its vector-tile-decoding work in a Web Worker via its own relative `new Worker(new URL(...))`; Vite's dev-server dependency pre-bundling rewrites/relocates that import, and the rewritten worker URL 404s at runtime — the map style, sprite and tile-source *metadata* all still load fine (so `tiles.openfreemap.org` requests looked completely healthy), but no vector tile ever got decoded into pixels, so the map area rendered as blank paper color forever. **Fix 2**: `vite.config.js` now excludes `maplibre-gl` from dependency pre-bundling (`optimizeDeps: { exclude: ['maplibre-gl'] }`), which stops Vite from touching its worker file. Confirmed fixed end-to-end: after `rm -rf node_modules/.vite` (to drop the stale pre-bundle) and reloading, the same Playwright script showed real `.../planet/13/.../....pbf` tile requests succeeding and a screenshot with actual streets/labels rendered under the pins, not blank. Interesting note for whoever hits this next: `npx vite build --mode production` had "worked" the whole time this bug existed in dev — Rollup's production module interop synthesizes a `.default` even for a module with none, masking the exact bug that broke the dev server. **A clean production build passing is not proof `maplibre-gl`'s import actually works in dev** — this only surfaces by actually opening the dev server in a browser (or Playwright) and checking for a runtime error, not from `vite build` succeeding.

Re-verified after both fixes: fast Playwright suite (`--grep-invert "real backend"`, chromium) — 92/92 passed. `npx vite build --mode production` — still clean. iOS Debug `xcodebuild` — clean.

## Follow-up (2026-09-16): iOS "Tìm ở đây" centering bug; in-map pin/list selection + preview card (both platforms)

### Task 1 — iOS "Tìm ở đây" alignment

**Root cause**: `MapExploreView.swift`'s top button row was `HStack { Button(Close); Spacer(); if boundsChanged { Button(SearchHere) }; Spacer(); Button(Compass) }` — SwiftUI splits an `HStack`'s two `Spacer()`s' available width equally, which centers the middle child in whatever space is left over *after* subtracting Close's and Compass's own widths, not the screen's true center. Close ("← Đóng"/"← Close", a text button) and Compass (a fixed 38pt circle) are different widths, so that leftover space was never symmetric — the button sat visibly off toward Compass's side (the narrower neighbor), matching the reported "shifted slightly to the right."

**Fix**: pulled `SearchHere` out of that `HStack` entirely and made it an independent child of the same `ZStack(alignment: .top)` the whole screen is already built on. A ZStack child with no explicit horizontal frame is centered by the ZStack's own alignment (`.top` = center-x, top-y) against the *entire* screen width — independent of Close/Compass's widths, safe-area insets, or sheet state, and with no hardcoded x-offset. `apps/ios/BanbeApp/Views/MapExploreView.swift` (HStack now just Close/Spacer/Compass; the `if boundsChanged { Button(searchHere) }` block moved to its own top-level `ZStack` child, same `.padding(.top, 8)` to keep its prior vertical position).

**Web**: audited, no change needed — `src/screens/MapExplore.jsx`'s search-here button already uses `left: '50%', transform: 'translateX(-50%)'`, which centers it against the full container width directly, entirely decoupled from the back/compass buttons' own (separately, absolutely positioned) placement. There was never an equivalent bug there.

### Tasks 2-4 — in-map event selection (pin + list), camera zoom, pop, compact preview card

**Reused, not duplicated**: both platforms' selection is just a `selectedId`/`selectedId` string read back against the *same* `events`/`visibleEvents` (web) or `app.mapEvents`/`visibleEvents` (iOS) this screen already loads and filters — no second query, no new event model. The preview card's photo/date-time/price/sold-out fields are the same cosmetic-catalogue join `fetchLiveEvents()` (web) / `EventCatalog.find(ev.id)` (iOS) already established for the list row, extended with two more joined fields (`when`, a live-seats-aware `soldOut`) rather than a new data path.

**Web** (`src/screens/MapExplore.jsx`):
- `fetchLiveEvents()` now also returns `when` (`cosmetic?.when` — the exact same display string the list/card already show elsewhere in the app, not a new date formatter) and `soldOut` (live `seats_remaining <= 0` when the row has one, falling back to the static catalogue's flag otherwise — mirrors the `urgent` field this screen already computed the same way).
- New `selectedId` state, `selectedEvent` memo (derived from `visibleEvents`, so it's automatically filter-aware), and an effect that clears `selectedId` the instant it's no longer in `visibleEvents` (Task 4 — filters/live updates never leave a stale card up).
- New `selectEvent(ev)` — sets `selectedId` and calls `map.flyTo({ center, zoom, padding })`, where `padding.bottom` accounts for both the sheet's current pixel height (`window.innerHeight * (1 - mapStripFraction)`, the exact same fraction that positions the sheet itself) and a fixed allowance for the new preview card, so the selected pin lands above both rather than at the screen's raw geometric center.
- Pin markers: `isSelected` styling (scale 1.15, an ink ring) applied at marker-creation time; a new `gocPinPop` CSS keyframe (`src/index.css`, same easing convention as `gocIn`/`gocSheetIn`) plays once via a `lastAnimatedPinIdRef` guard — only on an actual new selection, never replayed by the 5s poll's marker rebuild while the same pin stays selected. Marker click now calls `selectEvent(ev)` (was `goEvent(ev.id)` directly) with `e.stopPropagation()`.
- `map.on('click', () => setSelectedId(null))` added once at map init — fires only for a genuine tap on the map background (pins are separate marker DOM elements the click never reaches), never for a pan/zoom (`moveend`, unrelated) or the explicit Search-here button, so no accidental cross-triggering either way.
- List row `onClick` now calls `selectEvent(ev)` (was `goEvent(ev.id)` directly) — a list tap surfaces the same compact card instead of jumping straight to the full screen.
- New compact card JSX, positioned `bottom: calc(${100 - sheetTopVh}vh + 10px)` — anchored to the sheet's own current top edge (the same `sheetTopVh` that positions the sheet), so one rule places it sensibly in both List lớn (upper area, clear of the sheet's controls) and List nhỏ (a thin strip just above the peeking sheet) without two special-cased layouts. Shows thumbnail/name/when+area/price/sold-out state, a "×" close (`clearSelection`), and the CTA (`data-testid="map-card-cta"`) — the only thing that calls `goEvent(selectedEvent.id)`, i.e. the only path to the full-screen Event Detail screen from this flow.
- Each marker element also gets `data-testid="map-pin-<id>"` (new — was unlabeled) purely for test targetability.

**iOS** (`apps/ios/BanbeApp/Views/MapExploreView.swift`):
- New `selectedId`/`poppingId` state; `selectedEvent` computed from `visibleEvents`; a `sheetFraction` computed property that reads the currently-active `sheetDetent` back out by comparing it against the three literal `.fraction(...)` values `sheetContent` already declares (a `PresentationDetent` doesn't expose its own numeric fraction, but it is `Equatable`, so this works without tracking a second, parallel piece of state).
- `selectEvent(ev)` — since SwiftUI's `Map` has no pixel-padding primitive analogous to MapLibre's `flyTo({padding})`, this approximates the same intent by shifting the target `MKCoordinateRegion`'s center latitude southward (screen-down) by an amount proportional to how much of the screen the sheet currently covers, so the point lands in the upper part of the visible strip instead of the screen's raw center — a deliberate approximation, documented as such in-code, not a literal pixel-perfect port of the web behavior (the platforms' APIs don't match here). The pop is two sequenced `withAnimation` state changes (scale to 1.32, then settle to 1.15 ~160ms later) rather than a literal multi-stage keyframe (SwiftUI has no bare CSS-keyframe equivalent without the heavier `KeyframeAnimator` API), gated by `isNewSelection` so it never replays on a poll refresh while the same pin stays selected.
- `.onTapGesture { selectedId = nil }` added directly to the `Map(...)` view for background-tap-to-dismiss — coexists with `Map`'s own built-in pan/pinch gestures (not a replacement for them); this specific interaction is the one thing in this whole pass that couldn't be verified from this sandbox (no simulator UI interaction available) and is called out explicitly in the manual checklist.
- `pin(for:)` now calls `selectEvent(ev)` (was `app.goEvent(ev.id)` directly) and applies the same selected/popping `.scaleEffect` scaling as web's marker styling.
- List row's `.onTapGesture` now calls `selectEvent(ev)` (was `app.goEvent(ev.id)` directly).
- `.onChange(of: visibleEvents.map(\.id))` clears a `selectedId` that's dropped out of the filtered set — the same Task 4 guarantee as web's effect.
- New `selectedCard(_:)` view — same fields as web's card, joins `EventCatalog.find(ev.id)` for photo/when/price/soldOut via `CatalogPhoto` (the same component/loader path every other card in the app already uses, not a new one), CTA calls `app.goEvent(ev.id)`. Positioned via `.padding(.bottom, UIScreen.main.bounds.height * sheetFraction)` inside a bottom-anchored `VStack` — same "tied to the sheet's own current fraction" placement rule as web, so it works out at every detent without per-detent special-casing.

**A note on what "honor all active filters" means on this screen today**: the ticket's phrasing (category, area, attending, saved, sold out, nearby) is Home's filter set (note 12), not this screen's — Map Explore only ever had `catFilter`/`openNowOnly`/`sortByDistance-once-location-is-granted`. No new filter chips were added here (that would be a materially bigger change than this ticket's five tasks describe, and Task 4 explicitly says "do not add a second query/data source," not "add new filter UI"); "honor filters" is implemented as "the selection always tracks `visibleEvents`, whatever currently produces it," which is the entire actual filter surface this screen has.

### Task 5 — tests

**Web**: new `tests/map-explore.spec.js` (7 cases, all passing against the real `public.events` table the seeded demo rows already populate — no service-role/real-backend session needed, same public RLS the rest of this screen already relies on): pin tap shows the card; list-row tap shows the card without navigating away; the card's CTA opens the real Event Detail screen; a map-background tap dismisses the card; a plain background tap with no prior selection never shows "Search here" on its own; the card's "×" clears the selection; toggling "Còn chỗ" clears a selection that drops out of the filtered list. Fast suite re-run afterward: 99/99 (92 pre-existing + 7 new).

**iOS**: no automated UI test added. This app's existing `BanbeAppUITests` suite is already documented as broken from before this ticket (note 09's TODO: every test in it asserts a signed-out launch lands on `.home`, which stopped being true once login became mandatory — never fixed), and the swipe-back fix in this same session found XCUITest's synthetic gestures don't reliably exercise custom SwiftUI `DragGesture`/`.onTapGesture` code at all (a test that passed identically whether the real fix was present or not was removed rather than kept as false confidence). Building a new UI test on top of that same known-unreliable harness would be worse than not testing it — so the "most reliable available" coverage here is the manual checklist below, stated as such rather than padded with a test that wouldn't actually verify the behavior. `centerOnDensityHotspot`'s underlying `densityHotspot(_:)` free function already has web-side unit-test-equivalent coverage (`tests/density-hotspot.spec.js`) for the shared algorithm; the new `selectEvent`/`sheetFraction` logic is small enough to review by inspection (see file:line above) but isn't independently exercised by any Swift test target, since none exists in this project (`BanbeAppUITests` is UI-only — no plain XCTest logic target to add a lightweight one to without new project-level infrastructure, judged out of scope for this ticket).

**iOS manual verification checklist**:
1. Open Map Explore, tap a pin — camera animates/zooms toward it (not an instant jump), the pin visibly grows and settles slightly larger than the others, and the compact card appears above the sheet without hiding the pin.
2. Tap a second, different pin — the first pin returns to its normal size, the second one pops or the card updates to the second event, and the camera re-centers toward the new one.
3. Tap the map background (not a pin) — the card disappears and the previously-selected pin returns to normal size.
4. Tap a row in the list (both List lớn and List nhỏ) — the same card appears and the app stays on Map Explore; it must NOT jump to the full-screen Event Detail page.
5. Tap "Xem chi tiết" on the card — this is the only action that opens the real, full-screen Event Detail screen.
6. With the card showing, drag the sheet between its three detents (0.12/0.45/0.72) — the card should stay sensibly placed (visible, not overlapping the sheet's own controls) at each one.
7. Confirm dragging/panning/pinching the map itself still works normally after these changes (the new `.onTapGesture` on `Map(...)` for background-tap-to-dismiss is the one change in this pass most likely to interfere with Map's own built-in gestures if it does at all — this is the single highest-priority item to check on a real device/simulator).
8. Toggle the "Còn chỗ" (open-now) filter or the category filter while an event is selected — if the selected event drops out of the filtered set, the card should disappear on its own rather than keep showing stale data.

## Follow-up fix (2026-09-16): base map tiles still didn't paint in a production build (only pins showed)

**Checked, in the order asked, before concluding anything**:
1. Container size / `map.resize()` — measured the canvas's real bounding box via Playwright (`page.locator(...).boundingBox()`): always `1280×720`, never zero, in every state (`tall`/`peek`, before/after toggling). `mapDivRef`'s div is `position:absolute; inset:0`, fixed to the full screen regardless of the sheet's snap state — the sheet only visually overlaps part of it, it never resizes the map's own container. So there was never actually a zero/mis-sized-container problem here, and no `map.resize()` call was needed for that reason (ruled out, not guessed).
2. Style JSON — `curl`'d `https://tiles.openfreemap.org/styles/positron` directly again: still resolves, still references `tiles.openfreemap.org/planet` (vector) with no key required. Confirmed **not** the cause.
3. z-index/CSS layering — not the cause either (see below); the canvas element itself was present and correctly sized, it simply never had any pixels painted into it.
4. Console — the actual runtime error was found here, but not as a `console.error`/`pageerror`: reproduced by running `npx vite build && npx vite preview` (a real production build) under Playwright with `page.on('worker', ...)` — **`vite build` had never actually been checked at runtime before this**, only that it compiled — and a worker at `.../maplibre-gl-worker.mjs` (or, after the first attempted fix, a correctly-hashed `.../assets/maplibre-gl-worker-<hash>.mjs`) was created and then **immediately closed**, without ever surfacing as a page error.

**Root cause**: maplibre-gl computes its worker's URL at runtime as `new URL(\`./${t}\`, import.meta.url)` where `t` is a *variable* (`"maplibre-gl-worker.mjs"` chosen conditionally, not a static string literal) — Vite/Rollup's special `new Worker(new URL(...))` bundling detection only fires for a literal string, so it never recognizes or bundles this worker at all. A production build therefore never emits a file at that path; the URL 404s (masked as an HTTP 200 of `index.html` by `vite preview`'s SPA fallback, which the browser then fails to parse as a JS module, silently tearing the worker down). The style/sprite/source *metadata* all still load fine over plain `fetch` from the main thread — that's what made `curl`-testing the style URL and watching network responses look completely healthy — but with no live worker, no vector tile is ever decoded into pixels, so only the DOM-based markers (unrelated to the WebGL canvas) show up. This is dev-vs-prod-only: `vite dev` never hashes/relocates output files the way a build does, so it never exhibited this — the earlier fix in commit 9fa218e (excluding `maplibre-gl` from `optimizeDeps`) was correct for the dev-only worker bug it targeted, but never touched this separate, production-only one.

**First attempt (kept, but not sufficient alone)**: `import('maplibre-gl/dist/maplibre-gl-worker.mjs?url')` + `maplibregl.setWorkerUrl(...)` — makes Vite emit the worker file as a real, correctly-hashed build asset instead of a broken guessed path. This got the worker *created*, but it still closed immediately: the raw worker file itself does `import ... from './maplibre-gl-shared.mjs'`, and a bare `?url` copy is verbatim/opaque — it doesn't bundle or even look at the file's own imports, so once alone in a hashed `assets/` directory, that relative import 404s inside the worker too.

**Actual fix — `vite.config.js`'s new `maplibreWorkerFiles()` plugin**: copies `maplibre-gl-worker.mjs` **and** its sibling `maplibre-gl-shared.mjs` from `node_modules/maplibre-gl/dist/` verbatim (unhashed, original relative names preserved) to a fixed path — a dev-server middleware (`configureServer`) serving them at `/maplibre-gl-worker/*` in dev, and a `closeBundle` copy into `dist/maplibre-gl-worker/` for a production build — so both files sit next to each other exactly as the worker's own relative import expects, in both modes, without special-casing `MapExplore.jsx` per environment. `src/screens/MapExplore.jsx` now just calls `maplibregl.setWorkerUrl('/maplibre-gl-worker/maplibre-gl-worker.mjs')` unconditionally before constructing the map.

**Verified**: `npx vite build && npx vite preview --port 5173` (so Playwright's `reuseExistingServer` picks up the production build instead of the dev server) under a fresh Playwright run — `page.on('worker', ...)` shows the worker created and staying alive (previously: created then instantly closed); the resulting screenshot shows real streets/labels rendered under the pins, not a blank/gray strip. Re-ran with the normal dev server too (`--grep-invert "real backend"`, chromium) — 92/92 still pass, so the dev-mode fix from commit 9fa218e is untouched/still working.
