# Organizer participant-list, verification detail, and check-in confirm (2026-09-17)

## Status: WORKING (web + iOS)

## Files / functions
- `src/screens/Attendance.jsx` / `apps/ios/BanbeApp/Views/AttendanceView.swift` — the organizer's per-event guest list (`openAttendance`/`loadAttendanceGuests`).
- `src/screens/Verifications.jsx` / `apps/ios/BanbeApp/Views/VerificationsView.swift` — the organizer's PHASE 2 (`pending_verification`) queue (`v_pending_verifications`).
- `src/screens/sheets/QrScanSheet.jsx` / `apps/ios/BanbeApp/Views/QRScannerView.swift` — QR-scan check-in.
- `src/screens/PaymentDetails.jsx` / `apps/ios/BanbeApp/Views/PaymentViews.swift` — the guest's own PHASE 2 screen.
- `src/screens/sheets/ReasonSheet.jsx` / `apps/ios/BanbeApp/Views/Sheets.swift`'s `ReasonSheetView` — the shared "pick a reason" (or plain confirm) modal.
- `supabase/migrations/20260920000059_059_reject_pending_guest_and_nudge_organizer.sql` — new `nudge_organizer()` and `reject_pending_guest()` RPCs, `bookings.nudge_count` column.

## Bug 1 — 8bca6b1's own openNotification() fix had no per-event ownership check (confirmed real, fixed)

**Hypothesis confirmed**: `openVerifications()` (web `GocContext.jsx`, iOS `canHost` guard) only ever checked ACCOUNT-level state (`organizerMode || accountType === 'admin' || hasHosted`) — "is this person an organizer of *anything*" — never whether they're the organizer of the SPECIFIC event a `payment_awaiting_verification` notification's `event_id` names. `openAttendance()` (the `booking_requested` branch) had no ownership check at all, not even the account-wide one. For a dual-role account — this app's own explicit design ("one account for everything, switch on organizer mode from Account") and exactly what the shared fast-suite test account is — a guest who is also *some* event's organizer could tap/receive a `payment_awaiting_verification`/`booking_requested` notification about an event they don't organize and still land on the organizer's screen for it, RLS silently returning zero rows as the only real backstop.

**Fix**: `openNotification()` (web `GocContext.jsx:2664-2777`; iOS `AppState+Data.swift:530-`) now checks `n.data.event_id` against `s.myOrgEventKeys`/`myOrgEventKeys` (the real per-event ownership list from `loadMyEvents()`, already existed) before calling `openVerifications()` or `openAttendance()` — a non-owner is blocked from the navigation itself, not just left looking at buttons that silently no-op under RLS. `openVerificationDetail()` (new, below) carries the same guard for its own reachable-from-a-notification path.

## The guest's replacement screen (was the confusing part of Bug 1)

Rather than ever risking the organizer's own screen, `PaymentDetails.jsx`/`PaymentViews.swift`'s existing PHASE 2 card (already guest-framed — "Đang chờ xác nhận"/"your seat is locked") now also shows:
- A read-only countdown of `verify_due_at` (the organizer's own SLA/confirm-window deadline) — explicitly NOT a second "your seat is at risk" clock, the file's own long-standing top comment about PHASE 2 having no countdown is about that clock, not this new informational one.
- Exactly ONE actionable control: "Nhắc người tổ chức xác nhận" / "Remind the organizer to confirm" — `nudgeOrganizer()` (web) / `AppState.nudgeOrganizer(bookingID:)` (iOS), calling the new `nudge_organizer()` RPC.

**`nudge_organizer(p_booking uuid)`** (migration 059): guest-only (`bookings.user_id = auth.uid()`), only while `payment_state = 'pending_verification'`, rate-limited to **2** uses per hold via a real `bookings.nudge_count int default 0` column (not client-side debouncing, which resets on reload/reinstall) — the 3rd call is refused server-side with `NUDGE_LIMIT_REACHED`, and the button disables itself once `nudge_count` reaches 2. Inserts a `notifications` row (`kind: 'payment_verification_nudge'`) to the event's organizer. This app has no real push infra (07-notifications.md: no device-token table, no APNs key) — "push notification" here means the same in-app toast + bell notification every other event in this lifecycle already uses, surfaced proactively within one 5s toast-poll cycle either way.

## Bug 2a — "Check payment" (repurposed from the check-in toggle, for not-yet-paid guests)

An unpaid guest's row (`!g.paid`) is no longer tap-to-check-in at all (it used to be, regardless of payment state — check-in never made sense before payment was even reviewed). Instead:
- `!g.paid && !g.hasProof` (still PHASE 1, no proof yet): a plain "Chưa thanh toán"/"Not paid yet" badge, non-interactive — nothing to check yet (`v_pending_verifications` only ever covers PHASE 2).
- `!g.paid && g.hasProof` (PHASE 2, awaiting the organizer's decision): a "Kiểm tra thanh toán ›"/"Check payment ›" link — `openVerificationDetail(bookingId, eventKey)` (web `GocContext.jsx`) / `AppState.openVerificationDetail(bookingID:eventKey:)` (iOS) — navigates straight to that ONE booking's own row in Verifications, not just the general queue.

**`openVerificationDetail`** sets `verificationsFocusBookingId`/`verificationsFocusBookingID`, which `Verifications.jsx`/`VerificationsView.swift` filter the queue down to — showing exactly the tapped booking whether it's the only pending item or buried far down a long list, with a "‹ Xem tất cả"/"‹ View all" link back to the full queue (`clearVerificationsFocus`/`app.verificationsFocusBookingID = nil`). Carries the same `myOrgEventKeys` ownership guard as Bug 1's fix, since this is reachable from a bell notification too, not just Attendance's own (already-scoped) list.

Color: reused the existing muted-`alert` convention already used for "Huỷ vé" on this exact screen (`alert` at reduced opacity, not full-strength) — per `.claude/notes/06-design-tokens.md`, `alert` is the one shared accent token and this is its established "distinct but subtle, not harsh" secondary-action pattern; no new color introduced.

## Bug 2b — "Có nhận khách này không?" (accept/reject, repurposed from "Mark as paid")

The old single "Mark as paid" pill (unpaid guest only) is now a labeled choice with two pills:
- **"Nhận"/"Accept"** (`"Khách đã gửi biên lai ▪︎ Nhận"`/`"Guest sent proof ▪︎ Accept"` when proof was submitted) — unchanged underneath: `markGuestPaid()` → `confirm_payment` RPC.
- **"Từ chối"/"Reject"** — `openRejectGuest()` opens the shared reason-prompt sheet (kind `'rejectGuest'`, new `REJECT_GUEST_REASONS`/`ReasonOption.rejectGuest` list: no seats left / doesn't match statement / suspected fraud / other — the same "always pick a reason, never free text" convention as undo-check-in/cancel-booking), submitting to the new **`reject_pending_guest(p_booking, p_reason)`** RPC.

**`reject_pending_guest()`** (migration 059) — modeled on `cancel_booking()`'s organizer-authorization shape (`reject_payment()`'s exact check: `organizers.owner_id/user_id = auth.uid()` OR admin), gated to `payment_state IN ('holding', 'pending_verification')` only (an already-confirmed booking goes through "Huỷ vé" instead, which already asks for a reason):
- Sets **both** `status = 'cancelled'` AND `payment_state = 'cancelled'` — `booking_holds_seat()` (026:112) already returns `false` for `status IN ('cancelled','expired')` regardless of `payment_state`, so `status` alone frees the seat back to the pool; `payment_state` is set the same way purely so the booking drops out of `v_pending_verifications` (which filters strictly on `payment_state = 'pending_verification'` — leaving it untouched, as `cancel_booking()` does, would have left a "rejected" booking still showing in the organizer's own queue).
- Clears `hold_expires_at` and `verify_due_at` — halts every pending timer, not just the visible status.
- Inserts the guest's chat message (`'system'` kind, into the existing guest/organizer `threads` row) AND a `notifications` row (`kind: 'booking_declined'`) — same dual-channel pattern `reject_payment()`/`cancel_booking()` already establish, reused rather than reinvented.
- A second reject/any other transition attempt on an already-terminal booking correctly refuses with `INVALID_STATE`.

**Verified end-to-end against a real booking** (throwaway script against production, cleaned up after — not committed): signed in as the shared fast-suite test account, put it in organizer mode, created a real throwaway event it owns, self-booked it (hold_seats), submitted payment proof (pending_verification), nudged twice (succeeded, `nudge_count` 1→2) then a third time (correctly refused `NUDGE_LIMIT_REACHED`), then called `reject_pending_guest` — confirmed: `status`/`payment_state` both flipped to `cancelled`, `hold_expires_at`/`verify_due_at` both cleared, the booking no longer appears in `v_pending_verifications`, a `booking_declined` notification row landed for the guest with the exact reason text, a matching system chat message landed in the guest/organizer thread, and a second reject attempt was correctly refused (`INVALID_STATE`). Cleaned up the throwaway booking/event and reset the test account's organizer mode back off afterward (the script's own `set_organizer_mode(true)` call had briefly left it on, which broke `payment-state-machine.spec.js`'s "stays hidden from a goer" test until reverted — full fast suite reruns 125/125 clean after).

## Bug 3 — confirm before check-in (both the manual list and QR scan)

A confirmation step now sits before `check_in_guest()` actually fires, on both check-in entry points:
- **Manual tap** (Attendance list): `toggleCheckin()`/`toggleCheckIn()` now opens a plain yes/no prompt (`reasonPrompt` kind `'confirmCheckin'` — no reason list, unlike the other three kinds) instead of calling the RPC directly. "Xác nhận"/"Confirm" → `confirmCheckin()`/`confirmCheckIn()`, which performs the actual `performCheckIn`/`performCheckIn(bookingID:)` (factored out of the old inline logic so both the confirm dialog and — unchanged — the reversal path share it).
- **QR scan**: `QrScanSheet.jsx`/`QRScannerView.swift` — a decoded code used to call `checkInByScan()` the instant it was read; now it pauses the scan loop and shows a local (not the app-wide `reasonPrompt`) confirm overlay first, since this screen is already its own full-screen modal. "Xác nhận"/"Confirm" performs the check-in and resumes scanning after the usual status flash; "Để sau"/"Not now" resumes scanning immediately with no check-in.

This only ever applies to an already-**paid** guest now (Bug 2a made an unpaid guest's row non-check-in-tappable at all), so the confirm step is universal for every real check-in action left, not conditional on payment state itself.

## 2026-09-17 — follow-up: three more bugs found testing 5e47520

**Bug A — Home's "Chờ bạn xác nhận thanh toán" ▪︎ "Payments awaiting your OK" banner miscounted the viewer's OWN booking (real bug, CONFIRMED via a real booking, not the dual-role false alarm this looked like at first)**: `loadVerifications()` (web `src/state/GocContext.jsx`, iOS `AppState+Payments.swift:666`) queried `v_pending_verifications` with **no organizer filter at all**, relying entirely on `bookings`' RLS to scope the result. But `bookings` RLS is an OR of `bookings_select_guest` (`auth.uid() = user_id` — a guest's OWN rows) and `bookings_select_host` (organizes the event) — so a plain guest's own `pending_verification` booking came back through the *guest* policy and got counted by `orgPendingCount` (`src/screens/Home.jsx:59`) as if it were an organizer-side item, rendering the organizer-phrased banner (`Home.jsx:203-211`) right on the booking-owner's own screen, tappable into `openVerifications()`. Confirmed for real: created a live paid-event booking on the shared test account (not an organizer of that event), drove it to `pending_verification` via `submit_payment_proof`, and queried `v_pending_verifications` unfiltered — the row came back — vs. filtered to `organizer_id IN (this account's own organizer ids)` — correctly empty.

Fixed by scoping `loadVerifications()` explicitly to `organizer_id IN (myOrganizerIds/myOrganizerIDs)` — the exact established pattern `loadOrganizerHoldingSummary()` (`GocContext.jsx`) and `loadDocuments()`'s `documentsRole === 'host'` branch already use for this same "RLS's OR shape can't tell 'my booking' from 'my organizer queue' apart" class of gap. Admins (`accountType === 'admin'`/`isAdmin`) skip the filter — `bookings_select_admin` RLS exists specifically so the platform desk sees every organizer's queue.

**Bug B — first render after tapping "Tôi đã chuyển khoản" then navigating away and back could still show the stale 'holding' countdown**: `submitPaymentProof()` (web `GocContext.jsx`, iOS `AppState+Payments.swift:538`) already `await`ed `loadPaymentBookings()` before returning, and `PaymentDetails.jsx`/`PaymentViews.swift` already fetch fresh on mount — so this wasn't a missing-fetch bug so much as a real render/race gap: nothing guaranteed the freshly-fetched `pending_verification` data couldn't be raced by a slower in-flight fetch from just before the RPC resolved. Fixed per this ticket's own prescribed approach regardless of the exact race: `submitPaymentProof()` now patches the matching `paymentBookings`/`PayableBooking` entry in place **immediately** on success (`payment_state → pending_verification`, `transaction_id`, and `verify_due_at` straight from the RPC's own response, which already returns it) — before the `loadPaymentBookings()` refetch, not instead of it. `PaymentDetails.jsx`/`PaymentViews.swift` derive their rendered booking directly from this array on every render, so the very next render is correct with no dependency on any network round trip finishing first. Verified via the same real booking above: `submit_payment_proof`'s response carried `verify_due_at`, and the `bookings` row itself confirmed `payment_state: 'pending_verification'` immediately after.

**Bug C — "no way to undo a check-in" — investigated, NOT reproducible at HEAD, no code change made**: the ticket's premise (tapping an already-checked-in guest's row does nothing) does not match current code. `Attendance.jsx`/`AttendanceView.swift`'s row tap already routes an already-checked-in guest (`rowClickable = g.paid`, true for a checked-in guest) through `toggleCheckin(g.id, g.checkedIn)` → `if (checked) { openUndoCheckin(bookingId); return; }` (`GocContext.jsx`) → the existing `reasonPrompt` kind `'undoCheckin'` sheet → `submitReasonPrompt()` calls the pre-existing `undo_check_in(p_booking_id, p_reason)` RPC (`supabase/migrations/20260911000022_022_undo_checkin_and_cancel_notifications.sql:39`, which is also what `api/notify.js`'s `checkin_undo` case, formerly `api/notify-checkin-undo.js`, is triggered from). Same wiring confirmed on iOS (`AppState+Data.swift`'s `toggleCheckIn`/`.undoCheckin` case). This is already a confirm-before-commit flow (a reason must be picked, same shape as reject-guest/cancel-booking), so it already satisfies the "needs the same confirm-dialog pattern" ask too. No regression found — flagging in case the report came from a build predating 5e47520, or a UI state where `g.paid` was unexpectedly false for a checked-in guest (not reproduced).

**Third place audited (per this ticket's own ask, to avoid a fourth report of the same root cause)**: `loadOrganizerHoldingSummary()` and `loadDocuments()` (`GocContext.jsx`) both already scope to `myOrganizerIds` correctly — `loadVerifications()` was the one place this had been missed. No other query against an organizer-scoped view/table was found lacking this filter; `openVerifications()`'s own account-level gate (`organizerMode || accountType==='admin' || hasHosted`) is unrelated to this data-leak class and was already addressed (per-event) in the Bug 1 section above.

**Known leftover, not touched**: the shared fast-suite test account still owns an orphaned `organizers` row (`org_0c637bb2`, zero events) from an earlier session's throwaway verification script — harmless (contributes no rows to any organizer-scoped query) but left as-is since deleting it wasn't in scope here and organizer rows may have FK dependents elsewhere.

## 2026-09-17 — follow-up: three more bugs, all confirmed real

**Bug 1 — accepting a guest never flipped `payment_state`, so every "awaiting" surface lingered (real bug, root cause in SQL, not the client)**: `confirm_payment()` (`supabase/migrations/20260913000024_024_payments_and_documents.sql:448`) predates the two-phase `payment_state` machine (migration 026) entirely and was never updated after it landed — it only ever set `status = 'confirmed'`. Every screen added since 026 (`PaymentDetails.jsx`/`PaymentViews.swift`'s `isPending`/`.pendingVerification` cards, `Home.jsx`'s `myPendingVerification`/`orgPendingCount` banners on both sides, `v_pending_verifications` behind `Verifications.jsx`) reads `payment_state`, not `status` — so accepting a guest via "Có nhận khách này không?" ▪︎ "Nhận" correctly flipped `status` but left `payment_state` at `'holding'`/`'pending_verification'`, so all of them kept rendering as still-pending.

Fixed in `supabase/migrations/20260921000060_060_confirm_payment_sets_payment_state.sql` — `confirm_payment()` now also sets `payment_state = 'confirmed'` and clears `hold_expires_at`/`verify_due_at`, the same halt-every-timer shape `reject_pending_guest()` (059) already established. Client-side, `confirmPayment()`/`markGuestPaid()` (`GocContext.jsx`, `AppState+Payments.swift`) now also patch `paymentBookings`, `verifications` (removes the row), and the top-level `booking` in place immediately on success — the same reasoning as Bug B below: nothing should depend on a poll/refetch to stop showing a dead countdown.

**Verified against a real booking** (throwaway script, cleaned up after): created a real event owned by the test account, self-booked it, submitted proof (now `pending_verification`, present in `v_pending_verifications`), called `confirm_payment` — confirmed `payment_state`/`status` both flip to `confirmed`, `hold_expires_at`/`verify_due_at` both cleared, and the row immediately drops out of `v_pending_verifications` — all server-side, so the client-side patches above mean Home's banners and the guest's own countdown disappear on the very next render, not the next 6s poll tick.

**Bug 2 — nudge caption**: added a caption under the nudge button, shown only while `nudgeCount < 2` (once the limit is hit, the existing "Đã nhắc tối đa 2 lần" disabled label already says so) — `src/screens/PaymentDetails.jsx` (`data-testid="payment-nudge-limit-note"`), `apps/ios/BanbeApp/Views/PaymentViews.swift` (`payment.nudgeLimitNote`).

**Bug 3a — rejected/cancelled booking had no dedicated view, fell through to the wrong fallback**: `payment_state = 'cancelled'` (the terminal state `reject_pending_guest()` puts a booking in) matched no branch in either `PaymentDetails.jsx` or `PaymentViews.swift`'s phase switch. Web simply rendered nothing below the header (default title, no card). iOS was worse: its phase switch has no `.cancelled` case either, so it fell into the generic `else if !booking.hasAnyPayRail` branch and showed "Người tổ chức chưa thêm thông tin nhận tiền..."/"The organizer hasn't added payment details yet..." — a notice meant for an organizer who never configured a bank/MoMo rail, not a declined booking. Also: `reject_pending_guest()` stores its reason in `cancel_reason` (migration 059), but `loadPaymentBookings()`'s select list only fetched `dispute_reason` — `cancel_reason` was never even fetched on web; iOS's `PayableBooking`/`PayableBookingRow` had no `cancelReason` field at all.

Fixed: both platforms now select/decode `cancel_reason`, and both render a dedicated "Đã bị từ chối"/"Booking declined" card (`data-testid="payment-rejected"` / `accessibilityIdentifier("payment.rejected")`) showing that reason (or a "no reason given" fallback) plus a plain-language note that the seat was released — instead of the amount-only blank state (web) or the wrong pay-rail notice (iOS).

**Bug 3b — back navigation lingered / re-entry**: `backFromPaymentDetails()` (`GocContext.jsx`) and the `BackLink` handler (`PaymentViews.swift`) now check the booking's `payment_state` and go straight to `'home'`/`.home` when it's `'cancelled'`, instead of whatever `paymentBack`/`paymentBack` screen sent the guest here (which could itself still be transitioning off now-dead timer UI). Re-entry: `Home.jsx`'s `myHolding`/`myPendingVerification` banners already filter strictly on `payment_state IN ('holding', 'pending_verification')`, so a cancelled booking naturally can't produce a card that re-opens this screen; `booking_declined` was already confirmed non-deep-linked from `openNotification()` (07-notifications.md), so no notification tap resurrects it either. The live 6s poll (`PaymentDetails.jsx`'s `useEffect`, `PaymentViews.swift`'s `startPollingIfNeeded()`) now also stops once `isCancelled`/`.cancelled` is reached (previously only stopped at confirmed/expired) — a terminal state has nothing left to poll for — and iOS's poll patches `cancelReason` too, so a rejection caught live (guest actively on the screen when it happens) renders the reason immediately, not just the phase change.

`vite build` clean; iOS `xcodebuild` clean (Debug); migration 060 applied via `supabase db push`; fast Playwright suite 123/125, the 2 failures (`example.spec.js`, a different `map-explore.spec.js` test each run) reconfirmed pre-existing/flaky, unrelated to this change.

## 2026-09-17 — follow-up: nav-stack back leak, Going-tag staleness, QR-redirect + receipt-request

**Bug 1a — "Booking declined" back navigation had a SECOND, unfixed exit path**: 4be29bb's fix only patched `PaymentViews.swift`'s in-view `BackLink` closure. This app has no real `NavigationStack` (`RootView.swift`'s own comment: "switches one explicit `screen` at a time instead of using a NavigationStack") — but it has a *second*, separate exit path: a custom right-edge swipe gesture (`RootView.swift`, driven by `AppState.canSwipeBack`/`backTargetScreen`) that calls `AppState.goBack()`, whose own `case .paymentDetails: screen = paymentBack` (and `backTargetScreen`'s matching case, used to preview the destination mid-swipe) never had the cancelled check at all. Fixed by extracting a single shared `AppState.paymentDetailsBackTarget` computed property (checks `paymentBookings` for the current booking's `paymentState == .cancelled`) and pointing both `goBack()`, `backTargetScreen`, and `PaymentViews.swift`'s `BackLink` at it — one source of truth instead of two call sites that can drift out of sync again. Web has no equivalent second exit path (no `popstate`/history-stack wiring anywhere in `App.jsx`/`GocContext.jsx` — confirmed by grep — only `history.replaceState`, which creates no back-stack entries), so `backFromPaymentDetails()` alone was already sufficient there.

**Bug 1b — the "Going" tag never refetched on a reject, only on opening the Going tab**: confirmed via `loadMyEvents()` (web `GocContext.jsx`, iOS `AppState+Data.swift`) call-site audit — its only triggers are the sign-in mount effect and `goGoingList()` (80423dd's fix). `reject_pending_guest()`'s `submitReasonPrompt()` success path never called it, and `openNotification()` had no `booking_declined` case at all (tapping that bell notification did nothing beyond marking it read). Fixed at the one place that already runs regardless of whether the guest ever taps anything: the 5s notification/toast poll (`GocContext.jsx`, `AppState+Data.swift`'s `startNotificationPolling()`) now calls `loadMyEvents()` the moment it sees a fresh `booking_declined` row — proactive, not tap-gated, so Home's "Going" tag corrects itself within one poll cycle even if the guest never opens the notification or the Going tab. **Caused a real regression while implementing** (see below) — fixed and reverified before commit.

**Regression caught during this pass**: adding `loadMyEvents` to the toast-poll effect's own closure/deps put a *reference* to `loadMyEvents` inside an effect defined ABOVE `loadMyEvents`'s own `const ... = useCallback(...)` declaration further down the file — a temporal-dead-zone `ReferenceError` (`Cannot access 'loadMyEvents' before initialization`) thrown on every render, crashing `<GocProvider>` for every signed-out AND signed-in visitor alike. Caught by an 85-failure Playwright run across completely unrelated suites (onboarding, map-explore, auth) — the tell that something broke app-wide, not a narrow regression — then confirmed via a direct `page.on('pageerror', ...)` capture showing the exact ReferenceError and stack. Fixed by moving `loadMyEvents`'s entire definition (comment included) to before the toast-poll effect. Full fast suite reruns clean afterward (121/125, same 4 pre-existing/flaky failures as every other pass this session).

**Bug 2 — QR redirect on accept**: `PaymentDetails.jsx`'s live 6s poll / `PaymentViews.swift`'s `startPollingIfNeeded()` — on detecting `payment_state` flip to `confirmed` (an organizer accepting via Check-in, `confirm_payment()`, migration 060) — now call `openBookingConfirmed(bookingId, event_id)` instead of only patching local state, so a guest sitting on PaymentDetails at that moment lands straight on the ticket/QR screen (Confirmed.jsx/ConfirmedView.swift) instead of reading an inline "Đã xác nhận" card. Like every other live-update path in this app, this only fires while the guest's own client is actively polling (no real push infra — 07-notifications.md).

**Bug 2 — receipt request, new RPC**: no `request_receipt`/`receipt_request` RPC or notification kind existed anywhere before this (confirmed by repo-wide grep). New `request_receipt(p_booking uuid)` (`supabase/migrations/20260921000061_061_request_receipt.sql`) — modeled directly on `nudge_organizer()` (059): guest-only, requires `payment_state = 'confirmed'` (nothing to request before that), refuses if a live (non-superseded) `payment_documents` receipt already exists (`RECEIPT_ALREADY_EXISTS`), and rate-limits via a real `bookings.receipt_requested_at` column — 30-minute cooldown, not client debouncing — refusing a too-soon repeat with `ALREADY_REQUESTED_RECENTLY`. Inserts a `notifications` row (`kind: 'receipt_requested'`) to the event's organizer.

**Confirmed.jsx/ConfirmedView.swift** — new "Xem Receipt" control, shown only once `payment_documents` has actually been checked for this booking (`loadReceiptStatus(bookingId)`, a direct `booking_id`+`kind='receipt'`+`superseded_at IS NULL` query, not the general Documents list): opens the existing document viewer directly (`openDocumentFromNotification(id)`/iOS's method of the same name — the same "fetch by id, no need for the full list first" path already used for the `payment_document_uploaded` bell notification) if one exists, or calls `request_receipt()` if not.

**openNotification()'s new `receipt_requested` branch** (web `GocContext.jsx`, iOS `AppState+Data.swift`) — same per-event `myOrgEventKeys`/`iOrganize` ownership guard as every other organizer-bound kind (5e47520) — sets `attendanceHighlightBookingId`/`attendanceHighlightBookingID` and calls `openAttendance(event_id)`.

**Auto-highlight, confirmed working**: `Attendance.jsx`/`AttendanceView.swift` — a ref map (web) / `ScrollViewReader` + `.id(guest.id)` (iOS) scrolls the matching guest's "Upload receipt" control into view (`scrollIntoView`/`proxy.scrollTo`, `block: .center`) the moment the guests list has actually loaded (keyed off both the highlight id AND the guests array itself, since `openAttendance()` clears the list before reloading it — scrolling to a row that isn't rendered yet is a no-op), flashes it (an `alert`-colored border + soft fill for ~2.2s, mirroring `DisputeChatPanel.jsx`'s chat-message highlight timing), then clears the request so it doesn't refire on the next render. Same `alert`-token convention as every other emphasis on this screen — no new color.

**Verified end-to-end against real bookings** (throwaway script, cleaned up after): `request_receipt` correctly refused before confirmation (`INVALID_STATE`), succeeded once confirmed, correctly refused a second immediate call (`ALREADY_REQUESTED_RECENTLY`) with the right column/notification row landing for the organizer; a second, separately-rejected booking on the same event confirmed the "Going" query (`bookings` filtered to `pending/confirmed/attended`) no longer includes the rejected booking immediately after `reject_pending_guest()`, with its `booking_declined` notification row present (the exact signal the toast-poll fix above watches for).

Migration 061 applied via `supabase db push`. `vite build` clean; iOS `xcodebuild` clean (Debug); full fast Playwright suite 121/125 (4 pre-existing/flaky failures — `example.spec.js`'s stock template test, 2 `map-explore.spec.js` tests, reconfirmed unrelated by rerun).

## Design tokens used (06-design-tokens.md)

No new color was introduced anywhere in this pass. "Từ chối"/"Reject" and "Kiểm tra thanh toán ›"/"Check payment ›" both reuse the existing `alert` token at reduced opacity (`alert.opacity(0.8)`/`rgba` equivalent) — the same convention "Huỷ vé"/"Cancel booking" already established on this exact screen for a distinct-but-not-harsh secondary action.
